# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Project build guide

Read **BUILD.md** at the repo root first — it documents the architecture, navigation tree, design tokens, reusable primitives, the static-first mock-data layer (the backend swap point), the phased build log, the multi-agent workflows used, the brand-asset (icon/splash) pipeline, and the convention for adding a new screen.
